import { describe, it, expect } from 'vitest';
import * as defs from './app-route-defs';

describe('app-route-defs', () => {
  it('should export something', () => {
    expect(defs).toBeDefined();
  });
});
